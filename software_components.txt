Setting the Java JDK using SDKMAN:
==================================
use sdk, e.g.:
  sdk update            (update sdk itesel)
  sdk install Java      (installs latest version of java)
  sdk install scala     (installs latest version of scala)
